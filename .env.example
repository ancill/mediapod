# ===========================================
# Mediapod Configuration
# ===========================================
# Copy this file to .env and fill in your values

# -------------------------------------------
# Required: Domain Configuration
# -------------------------------------------
# These domains must point to your server's IP
MEDIAPOD_API_DOMAIN=media.yourdomain.com
MEDIAPOD_IMG_DOMAIN=img.yourdomain.com
MEDIAPOD_VOD_DOMAIN=vod.yourdomain.com
MEDIAPOD_S3_DOMAIN=s3.yourdomain.com

# -------------------------------------------
# Required: Secrets (generate secure values!)
# -------------------------------------------
# Database password (change this!)
DB_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# MinIO (S3) credentials
MINIO_ROOT_USER=mediapod
MINIO_ROOT_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# imgproxy signing keys (generate with: openssl rand -hex 32)
IMGPROXY_KEY=GENERATE_WITH_openssl_rand_hex_32
IMGPROXY_SALT=GENERATE_WITH_openssl_rand_hex_32

# -------------------------------------------
# Optional: Network Configuration
# -------------------------------------------
# Docker network name (default: mediapod-network)
# MEDIAPOD_NETWORK=mediapod-network

# Set to 'true' if using an external network (e.g., shared with Traefik)
# MEDIAPOD_NETWORK_EXTERNAL=false

# Traefik certificate resolver name
# TRAEFIK_CERTRESOLVER=myresolver

# -------------------------------------------
# Optional: Host Port Mappings
# -------------------------------------------
# These expose services on localhost for debugging
# POSTGRES_HOST_PORT=127.0.0.1:5433
# REDIS_HOST_PORT=127.0.0.1:6380
# MINIO_CONSOLE_HOST_PORT=127.0.0.1:9001

# -------------------------------------------
# Optional: MinIO Console URL
# -------------------------------------------
# If you want to access MinIO console via a domain
# MINIO_CONSOLE_URL=https://minio.yourdomain.com

# -------------------------------------------
# Optional: Worker Configuration
# -------------------------------------------
# Number of concurrent video processing workers
# WORKER_CONCURRENCY=2

# -------------------------------------------
# Optional: Custom Docker Images
# -------------------------------------------
# If publishing to a registry, override these
# MEDIAPOD_API_IMAGE=ghcr.io/ancill/mediapod-api:latest
# MEDIAPOD_WORKER_IMAGE=ghcr.io/ancill/mediapod-worker:latest
